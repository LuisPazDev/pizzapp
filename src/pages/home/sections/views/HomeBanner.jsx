import { Row, Col } from "react-bootstrap";
import { DeliveryCard } from "../components";

export const HomeBanner = () => {
  return (
    <Row xs={1} md={2}>
      <DeliveryCard />
    </Row>
  );
};
